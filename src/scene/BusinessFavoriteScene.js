import React from 'react';
import { View, Text, StyleSheet } from "react-native";

const BusinessFavoriteScene = () => {
    return (
        <View>
            <Text>Favorite Scene</Text>
        </View>
    );
};

const styles = StyleSheet.create({});

export default BusinessFavoriteScene;